# Versões travadas, e travadas nas MESMAS do portal-integra onde o pacote é
# comum: o código copiado de lá foi escrito e testado contra estas.

# A tela
Flask==3.1.3

# Configuração
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.2.2

# Destino — PostgreSQL do Portal. `execute_values` vem daqui, e é o que torna
# viável inserir centenas de milhares de linhas (ver Etapa 4).
psycopg2-binary==2.9.12

# Origem — Domínio (SQL Anywhere) via ODBC, somente leitura.
# EXIGE o driver ODBC do SQL Anywhere instalado na máquina, em 64 bits. Ele é
# proprietário e não vem por pip: é a restrição que decide onde este processo
# pode rodar, e a razão de ele existir separado do Portal.
pyodbc==5.3.0

# Agendamento da rodada automática. Entra em uso depois da Etapa 5; está aqui
# desde já para que a máquina do cliente seja preparada uma vez só.
APScheduler==3.10.4
